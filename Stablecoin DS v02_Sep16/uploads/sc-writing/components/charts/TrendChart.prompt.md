**TrendChart** is a compact SVG area+line for a rolling window (e.g. 90-day reserve ratio or supply), with an optional dashed floor line. No libraries.

```jsx
<TrendChart data={ratioSeries} floor={100} color="var(--brand-teal)" />
```
